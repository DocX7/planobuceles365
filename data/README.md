# Pasta de dados

Para exibir o texto bíblico completo no app, suba aqui o arquivo:

```txt
biblia-acf.json
```

O arquivo deve ficar exatamente neste caminho:

```txt
data/biblia-acf.json
```

Esse JSON foi gerado a partir do CSV `1. Bíblia ACF(1).csv` enviado no ChatGPT.
